// https://juejin.cn/post/7217267332656742460
// Custom styles.
// 整体背景设置
body {
 	background:url();      // 设定背景图片,images同处于source文件夹下
 	background-repeat: no-repeat;           // 设定背景图片非重复填充
    background-attachment:fixed;          // 设置背景图片不随页面滚动
    background-position:center;          // 设置背景图片位置
	background-size: cover                  // 设置保持图像的纵横比并将图像缩放成将完全覆盖背景定位区域的最小大小
}
//文章背板的颜色rgb
.main-inner > .sub-menu, .main-inner > .post-block, .main-inner > .tabs-comment, .main-inner > .comments, .main-inner > .pagination{
	background: #f5f5f56b;		//此处使用十六进制颜色代码，			也可以使用rgba进行调色，
    							//实际效果为白色透明色底板				rgba的第四参数即为透明度
}
body{				//修改主体字体颜色
  color: #000;		//纯黑
}
.posts-expand .post-title-link {	//标题颜色
    color: #000;				//首页文章标题颜色， （默认为灰辨识度不高）
}
.posts-expand .post-meta-container {	//标题下的日期颜色
    color: #880000;				//此处修改为红色，可自行调用rgb调色
}
.posts-expand .post-eof {
    display: none;    
}
// 页面头样式属性
.header-inner {
  // 也可以同时定义背景色
  background: #ddd 
  // 透明度
  opacity: 0.2;
}

//侧边框的透明度设置
.sidebar {
  opacity: 0.9;
}

//菜单栏的调色
.header-inner {		
  background: rgba(255,255,255,0.7);
}
//搜索框透明
.popup {		
  opacity: 0.93;
}

.main-inner {
    background-color: rgba(255, 255, 255, 0.0);  //主体背景透明
    padding: 0px 40px 40px 40px;  //调整组件位置
}
.post {
margin-top: 0px;
    margin-bottom: 25px;
    padding: 30px;
    -webkit-box-shadow: 0 0 5px rgba(202, 203, 203, .5);
    -moz-box-shadow: 0 0 5px #222
    border-radius:25px;  /* 矩形有圆角 */
}
